{
  "name": "cdn-management-server",
  "version": "1.0.0",
  "description": "CDN Management Server",
  "main": "src/server.ts",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "vitest": "^0.34.6",
    "supertest": "^6.3.3"
  }
}