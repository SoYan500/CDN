{
  "name": "cdn-management-server",
  "version": "1.0.0",
  "description": "CDN Management Server",
  "main": "src/server.ts",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "supertest": "^6.3.3",
    "vitest": "^0.34.6"
  }
}
